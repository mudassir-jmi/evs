<%- include('../partials/header') %> 

<link rel="stylesheet" href="/css/electnav.css">
<%- include('../partials/title') %> 
<%- include('../partials/vnav') %> 

<h1>Please create your wallet first</h1>
   <!-- // <div > -->
       <div class="wrapper1">
        
       <!-- // <div > -->
        <% if(!wallet.status) {%> 
        <form action="/v/wallet" method="post" class="card">
                <p>Please create your wallet first</p>
                <p>And a pdf <i class="fas fa-file-pdf"></i> will be sent, please keep it safe otherwise, you won't be able to recover it and not able give any vote ! <i class="fas fa-vote-yea" ></i></p >
            <input type="hidden" name="_csrf" value="<%=csrfToken%>">
            <div class="btn">
                
                <button type="submit" class="submit_btn" id="btnCandidate">create</button>
            </div>
        </form>
         <% } else { %>  

         <div >
           
            <br>
            <br>
            <form action="/v/balance" method="post" class="card">
                <h2>Hurray!</h2>
                <p>Wanna Check your available balance</p>
                <p>Open your pdf <i class="fas fa-file-pdf"></i> and type here your public key</i></p >
                    <div class="input_field">
                        <label>Public Key</label>
                        <input type="text" class="input" name ="pubkey" autocomplete="off">
                    </div>
           
            <div class="btn">
                <input type="hidden" name="electionId" value="<%=electionId%>">
                <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                <button type="submit" class="submit_btn" id="btnCandidate">create</button>
            </div>
        </form>
            </div>
            <% } %> 
            <%  if(showBalance) {  %> 
                <div class="card">
                    Your blanace is <%=balance%> bitvote
                </div>
                <% } %> 
       </div> 

<script src="/js/simple.js"></script>
<%- include('../partials/footer') %> 